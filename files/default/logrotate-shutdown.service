[Unit]
Description=Archive logs before shutdown
After=network.target

[Service]
RemainAfterExit=yes
ExecStop=/usr/sbin/logrotate /etc/logrotate.conf --force; /usr/local/bin/archive-logs.sh system

[Install]
WantedBy=multi-user.target
